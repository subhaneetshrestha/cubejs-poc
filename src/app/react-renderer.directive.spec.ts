import { ReactRendererDirective } from './react-renderer.directive';

describe('ReactRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactRendererDirective();
    expect(directive).toBeTruthy();
  });
});
